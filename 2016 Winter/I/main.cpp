#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

vector<int> g[2]

int main()
{
    int ncase;
    scanf("%d", &ncase);

    while(ncase--) {
        int n;
        scanf("%d", &n);

        for(int i = 0; i < n - 1; i++) {
            
        }
    }
    return 0;
}